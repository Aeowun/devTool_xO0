Htt I see the problem in the screenshot: Samsung's Clipboard History UI is open and is blocking the entire screen with a "Delete 40 items?" dialog. I am manually clearing this dialog and the keyboard now to return to a clean ChatGPT stateI see the problem in the screenshot: Samsung's Clipboard History UI is open and is blocking the entire screen with a "Delete 40 items?" dialog. I am manually clearing this dialog and the keyboard now to return to a clean ChatGPT stateRust enums let you define a type that can represent one of several possible variants.  The key idea is: an enum value is exactly one variant at a time, and each variant can optionally carry data.  Basic example  enum Direction { North, South, East, West, }  Now you can create a Direction:  let direction = Direction::North;  direction can be North, South, East, or West—but not multiple variants simultaneously.  Enums can carry data  This is where Rust enums become especially powerful:  enum Message { Quit, Rust enums let y: